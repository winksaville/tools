# Copyright 2015 wink saville
#
# licensed under the apache license, version 2.0 (the "license");
# you may not use this file except in compliance with the license.
# you may obtain a copy of the license at
#
#     http://www.apache.org/licenses/license-2.0
#
# unless required by applicable law or agreed to in writing, software
# distributed under the license is distributed on an "as is" basis,
# without warranties or conditions of any kind, either express or implied.
# see the license for the specific language governing permissions and
# limitations under the license.

language: python
sudo: false

python:
  - "3.5"

env:
  global:
    - INSTALL_DIR=$HOME/opt
    - BIN_DIR=${INSTALL_DIR}/bin
    - CROSS_BIN_DIR=${INSTALL_DIR}/cross/bin
    - CACHE_DIR=$HOME/cached
    - SRC_DIR=$HOME/tmp
  matrix:
    - Platform=Posix

cache:
  directories:
    - ${INSTALL_DIR}
    - ${CACHED_DIR}

addons:
  apt:
    packages:
      - texinfo # needed by binutils-gdb
      - libgmp-dev # needed by gcc
      - libmpfr-dev # needed by gcc
      - libmpc-dev # needed by gcc
      - re2c # Needed by ninja
        #- qemu-system-arm  # Doesn't install!!

before_install:
  - mkdir -p ${BIN_DIR}
  - mkdir -p ${CROSS_BIN_DIR}
  - export PATH=${BIN_DIR}:$PATH
  - export PATH=${CROSS_BIN_DIR}:$PATH
  - ls -al /bin/sh
  - which sh
  - sh --version
  - ls -al /bin/bash
  - which bash
  - if [ ! -L ${BIN_DIR}/sh ]; then ln -s /bin/bash ${BIN_DIR}/sh; fi
  - which sh
  - sh --version

#install:
  #- tools/ninja-install.py --src ${SRC_DIR}/ninja --prefix ${INSTALL_DIR}
  #- tools/qemu-install.py --src ${SRC_DIR}/qemu --prefix ${INSTALL_DIR}
  #- tools/binutils-install.py --src ${SRC_DIR}/binutils --prefix ${INSTALL_DIR}/cross
  #- tools/gcc-install.py --src ${SRC_DIR}/gcc --prefix ${INSTALL_DIR}/cross

script:
  - python --version
  - output=$(python --version 2>&1) && [[ ${output} == *"3.5"* ]] ; echo $? ; echo ${output}
  - python2 --version
  - output=$(python2 --version 2>&1) && [[ ${output} == *"2.7"* ]] ; echo $? ; echo ${output}
  - python3 --version
  - output=$(python3 --version 2>&1) && [[ ${output} == *"3.5"* ]] ; echo $? ; echo ${output}
  - virtualenv --version
    #- arm-eabi-ld --version
    #- output=$(ninja --version) && [[ ${output} == *"1.6"* ]]
    #- qemu-system-arm --version
    #- arm-eabi-gcc --version
